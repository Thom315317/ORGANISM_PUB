# Required for bench V7 and reanalysis
numpy>=1.24
scipy>=1.11.0
sentence-transformers>=2.2
ollama>=0.1.9
flask>=2.3
requests>=2.31

# Optional (graceful fallback if missing)
# umap-learn>=0.5    # UMAP projection in viewer_v3
